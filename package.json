{
  "name": "github-repo-sync",
  "version": "1.0.0",
  "description": "Sync GitHub repositories to different branches of a target repository",
  "main": "sync.js",
  "scripts": {
    "sync": "node sync.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "github",
    "sync",
    "repository"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@octokit/rest": "^20.0.2",
    "simple-git": "^3.24.0"
  }
}
